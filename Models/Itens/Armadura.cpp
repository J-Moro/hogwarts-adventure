#include "Armadura.h"

Armadura::Armadura(string name, int faBonus, int damageBonus){
    setName(name);
    setType(r);
    setCombat(true);
    setFaBonus(faBonus);
    setDamageBonus(damageBonus);
}